name: Apply V4 source registration UI

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.head.ref == 'feat/v4-source-registration-20260820'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/v4-source-registration-20260820
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - name: Apply UI patch
        run: node scripts/apply-v4-source-registration-ui.mjs
      - name: Validate
        run: |
          npm test
          npm run check
      - name: Commit functional UI
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/index.html
          git commit -m "feat: expose V4 source registration in admin"
      - name: Remove one-time tooling
        run: |
          rm -f scripts/apply-v4-source-registration-ui.mjs .github/workflows/apply-v4-source-registration-ui.yml
          git add -A
          git commit -m "chore: remove V4 source UI patch tooling"
      - name: Push
        run: git push origin HEAD:feat/v4-source-registration-20260820
