{
  "name": "telegram-channel-cloner",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Safe 1-to-N Telegram channel cloning with destination-specific internal-link rewriting.",
  "scripts": {
    "build": "node scripts/verify-env.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check lib/*.js && node --check api/*.js && node --check api/auth/*.js && node --check api/cron/*.js && node --check api/reader/*.js && node --check api/telegram/*.js && node --check server/admin/*.js",
    "check:domains": "node scripts/check-domain-isolation.js",
    "check:secrets": "! git grep -nE '(sb_secret_[A-Za-z0-9_-]{20,}|service_role.{0,20}[A-Za-z0-9_-]{24,}|[0-9]{8,}:[A-Za-z0-9_-]{30,})' -- ':!package.json' ':!package-lock.json' ':!scripts/check-domain-isolation.js' ':!.github/workflows/*'",
    "check:clone": "npm run check:domains && npm run check:secrets",
    "dev": "npx vercel dev"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@vercel/functions": "^3.9.5",
    "big-integer": "^1.6.48",
    "teleproto": "1.229.0"
  }
}
